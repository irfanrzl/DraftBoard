# Acme

Auto-generated single-page site, composed from a layout.

## Run it

```bash
npm install
npm run dev
```

Blocks: nav → hero → grid → feature → cta → footer

A starting skeleton — replace placeholder content with your own.
